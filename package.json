{
  "name": "root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:yoelio": "lerna run dev --scope=yoelio --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
